# .gitignore（理由付き）

# -----------------------
# Node（フロントエンド）
# -----------------------
node_modules/            # 依存パッケージの実体。巨大・OS依存。lockからいつでも再生成（pnpm i 等）
dist/                    # Vite 等のビルド成果物。ソースから自動生成されるため履歴不要
.next/                   # Next.js のビルド/キャッシュ出力（使っていなくても無害に除外）
out/                     # Next.js export などの出力先。生成物のため除外

# -----------------------
# Python（バックエンド）
# -----------------------
backend/.venv/           # Python 仮想環境の実体。マシン/パス依存。uv/venvで再作成できる
__pycache__/             # 実行時に生成されるバイトコードキャッシュ（*.pyc を格納）
*.pyc                    # 同上：バイトコードファイル本体。自動生成される
.pytest_cache/           # pytest の実行キャッシュ。再実行で再生成
.mypy_cache/             # mypy の型チェックキャッシュ
.ruff_cache/             # Ruff の静的解析キャッシュ

# -----------------------
# Env / Secrets（機密）
# -----------------------
*.env                    # APIキーやパスワードなど機密を含む。人/環境ごとに値が異なる
.env.*                   # .env.local / .env.dev など環境別ファイルも同様に除外
!.env.example            # ただし“見本”は共有してオンボーディングを容易にする

# -----------------------
# Editor / OS（ローカル依存のゴミ）
# -----------------------
.vscode/*                # 個人のエディタ設定・状態ファイル。人により差が出るため除外
!.vscode/extensions.json # 例外：推奨拡張の共有はOK（チームの足並みを揃える）
!.vscode/settings.json   # 例外：最低限の共通設定（改行LFなど）は共有
.DS_Store                # macOS のフォルダメタ情報。ソースと無関係なゴミ
